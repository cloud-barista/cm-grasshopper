### Job status
GET {{baseUrl}}/job/status/{{job_id}}

### Notes
# - `current_stage` 는 현재 상위 단계를 나타냅니다.
# - 예: `precheck`, `backup`, `backup_sync`, `restore`, `completed`

### Job log
GET {{baseUrl}}/job/log/{{job_id}}

### Job list
GET {{baseUrl}}/job/status
